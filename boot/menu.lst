# grub boot menu configuration file
# By default, boot the first entry.
default 0
# automatically after 30 s
timeout 5
# fallback to the second entry.
fallback 1

# 注意，这里是因为boot本来没写字段，写入DBR会破坏文件系统结构，所以放入/中让grub去找
# 正常的做法是写入DBR，然后chainloader +1进行boot[见grub manual]
title LSL/Kernel
    root (hd0,0)
    makeactive
    chainloader /hdboot.bin
